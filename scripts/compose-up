#!/usr/bin/env bash
set -euo pipefail

if [[ ! -f .env ]]; then
  echo "Missing .env. Copy .env.example to .env and configure its secrets first." >&2
  exit 66
fi

docker compose --env-file .env up -d --build "$@"
docker compose --env-file .env rm -f database-migration
